#!/usr/bin/env Rscript

cat(
  "Tree scoring is implemented in scripts/12_score_trees.py so it can run\n",
  "without external tree libraries. Run:\n\n",
  "  python3 scripts/12_score_trees.py\n\n",
  sep = ""
)
quit(status = 1)
